{
    "environment": "development",
    
    "run_list":[
	"recipe[base]",
	"recipe[rsyslog::client]",
	"recipe[ldap::server]",
	"recipe[ldap]",
	"recipe[ldap::user]",
	"recipe[ldap::samba]",
	"recipe[munin]"
    ]
}
